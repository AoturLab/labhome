# labhome

Personal homepage hosted on GitHub Pages.

## Setup

This site is automatically published to GitHub Pages from the `main` branch.

To bind a custom domain:
1. Go to repository **Settings → Pages**
2. Set custom domain
3. Add DNS record (CNAME for subdomain or A records for apex domain)

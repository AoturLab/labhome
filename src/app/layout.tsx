import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Long Chen — Developer",
  description:
    "Personal homepage of Long Chen — developer, creator, open source enthusiast. Building tools like bilibit.",
  openGraph: {
    title: "Long Chen — Developer",
    description: "Developer & creator building tools that matter.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
